 // FutureBuilder(
              //   future: _future,
              //   builder: (context, snapshot) {
              //     List<String>? displices = snapshot.data;
              //     if (snapshot.connectionState == ConnectionState.waiting) {
              //       return const Center(
              //         child: CircularProgressIndicator(),
              //       );
              //     } else if (snapshot.hasData && displices != null) {
              //       if (displices.isNotEmpty) {
              //         return ListView.builder(
              //             shrinkWrap: true,
              //             itemCount: displices.length,
              //             itemBuilder: (context, int index) {
              //               return ListTile(
              //                   leading: const Icon(Icons.manage_history),
              //                   title: Text(displices[index]),
              //                   trailing: const Icon(Icons.arrow_forward),
              //                   onTap: () {
              //                     Navigator.pushNamed(
              //                         context, 'filterQuestions');
              //                   });
              //             });
              //       }
              //     } else if (snapshot.hasError) {
              //       return const Text('Erro ao carregar as disciplinas.');
              //     }
              //     return const Text('Nenhum disciplina cadastrada.');
              //   },
              // ),